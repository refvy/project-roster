DATABASE_URL="postgresql://roster:roster@127.0.0.1:5432/roster"
AUTH_SECRET="dev-only-change-me-use-openssl-rand"
APP_URL="http://localhost:3000"
# Production: APP_URL="https://getskwad.com" (magic/share URLs; never the tau alias).
# Local/CI only. Unset on Vercel production — the on-page magic link is also
# forced off when VERCEL_ENV=production.
AUTH_DEBUG="true"
RESEND_API_KEY=""
EMAIL_FROM="Skwad <onboarding@resend.dev>"
# Production cron (Vercel sends Authorization: Bearer CRON_SECRET).
# CRON_SECRET=""
# PostHog (free). Key present = on. Leave empty so production stays silent
# until Dan adds a project key. Host defaults to https://us.i.posthog.com.
# NEXT_PUBLIC_POSTHOG_KEY=""
# NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
